package com.turnhub.android.ui.theme

import androidx.compose.material3.Typography

// Material3 default type scale. Customize once TurnHub has real typography
// direction; the default is legible and accessible on its own.
val TurnHubTypography = Typography()
