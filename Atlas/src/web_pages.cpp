#include "web_pages.h"

namespace TurnHubWeb {

const char PORTAL_HTML[] PROGMEM = R"HTML(
<!doctype html>
<html lang="en">
<head>
<meta charset="utf-8">
<meta name="viewport" content="width=device-width,initial-scale=1,viewport-fit=cover">
<meta name="theme-color" content="#0b0d11">
<title>TurnHub</title>
<style>
:root{color-scheme:dark;--bg:#0b0d11;--panel:#141820;--panel2:#1b202a;--line:#2b3240;--text:#f3f5f7;--muted:#9ca6b7;--good:#62d58a;--warn:#efc55a;--bad:#ef6a6a;--blue:#72a7ff}
*{box-sizing:border-box}html,body{margin:0;min-height:100%;background:var(--bg);color:var(--text);font-family:system-ui,-apple-system,BlinkMacSystemFont,"Segoe UI",sans-serif}body{padding:16px}.shell{width:min(1180px,100%);margin:auto}header{display:flex;justify-content:space-between;align-items:center;gap:12px;margin-bottom:16px;flex-wrap:wrap}.brand{font-size:1.8rem;font-weight:950}.sub,.small{color:var(--muted);font-size:.82rem;line-height:1.45}.right,.actions{display:flex;gap:8px;align-items:center;flex-wrap:wrap}.dot{width:10px;height:10px;border-radius:50%;background:var(--bad)}.dot.online{background:var(--good)}a.link,button,input{border:1px solid var(--line);background:var(--panel2);color:var(--text);border-radius:10px;padding:9px 12px;font:inherit}a.link{text-decoration:none;font-weight:800}button{font-weight:800;cursor:pointer}button.primary{background:#e7ebf2;color:#111318;border-color:#e7ebf2}button.good{border-color:rgba(98,213,138,.5);color:var(--good)}button.warn{border-color:rgba(239,197,90,.5);color:var(--warn)}button.bad{border-color:rgba(239,106,106,.5);color:var(--bad)}button:disabled{opacity:.4;cursor:not-allowed}.grid{display:grid;grid-template-columns:repeat(12,1fr);gap:12px}.card{grid-column:span 12;background:var(--panel);border:1px solid var(--line);border-radius:18px;padding:18px}.hero{min-height:245px;display:flex;flex-direction:column;align-items:center;justify-content:center;text-align:center}.eyebrow,.section{font-size:.78rem;color:var(--muted);font-weight:900;letter-spacing:.12em;text-transform:uppercase}.hero h1{font-size:clamp(2.8rem,10vw,6rem);margin:.2rem 0;line-height:.95}.hero p{color:var(--muted);font-size:1.05rem;margin:.35rem 0}.badges{display:flex;gap:8px;flex-wrap:wrap;justify-content:center;margin-top:12px}.badge{border:1px solid var(--line);border-radius:999px;padding:6px 10px;color:var(--muted);font-size:.76rem;font-weight:800}.badge.good{color:var(--good)}.badge.warn{color:var(--warn)}.badge.blue{color:var(--blue)}.metrics{display:grid;grid-template-columns:repeat(2,1fr);gap:9px}.metric,.device,.session-box,.player,.seat{background:var(--panel2);border:1px solid var(--line);border-radius:14px;padding:13px}.metric label{display:block;color:var(--muted);font-size:.72rem;font-weight:800;text-transform:uppercase}.metric strong{display:block;margin-top:4px;font-size:1.08rem}.players,.devices{display:grid;grid-template-columns:repeat(auto-fit,minmax(210px,1fr));gap:10px}.player.active,.seat.active{border-color:var(--blue)}.player.winner{border-color:var(--good)}.player.eliminated{opacity:.5}.player-name,.device-name,.session-title{font-size:1.08rem;font-weight:900}.mono{font-family:ui-monospace,SFMono-Regular,Consolas,monospace;font-size:.75rem;color:var(--muted);overflow-wrap:anywhere}.device-top,.session-head{display:flex;justify-content:space-between;gap:10px;align-items:flex-start}.device-state{font-size:.72rem;font-weight:900}.online-text{color:var(--good)}.offline-text{color:var(--bad)}.seats{display:grid;gap:8px;margin-top:10px}.seat{padding:10px}.seat-top{display:flex;justify-content:space-between;gap:8px}.slot{display:inline-flex;width:25px;height:25px;align-items:center;justify-content:center;border-radius:8px;border:1px solid var(--line);font-weight:950;margin-right:6px}.notice{border-left:3px solid var(--warn);background:var(--panel2);padding:12px 13px;border-radius:10px;color:var(--muted);line-height:1.45}.profile{display:grid;grid-template-columns:1fr auto;gap:8px;margin-top:12px}.status-row{display:flex;justify-content:space-between;gap:12px;padding:9px 0;border-bottom:1px solid var(--line)}.status-row:last-child{border-bottom:0}#toast{min-height:24px;margin-top:10px;color:var(--muted);font-size:.84rem;font-weight:700}@media(min-width:820px){.hero-card{grid-column:span 8}.table-card{grid-column:span 4}.session-card{grid-column:span 5}.devices-card{grid-column:span 7}.players-card{grid-column:span 7}.system-card{grid-column:span 5}}@media(max-width:600px){body{padding:12px}.card{padding:14px}.hero{min-height:215px}.profile{grid-template-columns:1fr}}
</style>
</head>
<body>
<div class="shell">
<header><div><div class="brand">TurnHub</div><div class="sub">Atlas table console</div></div><div class="right"><span id="dot" class="dot"></span><span id="connection" class="sub">Connecting</span><a class="link" href="/dev">Dev</a><a class="link" href="/update">Firmware</a></div></header>
<div class="grid">
<section class="card hero hero-card"><div id="eyebrow" class="eyebrow">TURNHUB</div><h1 id="heroTitle">READY</h1><p id="heroSub">Waiting for Atlas</p><div id="heroBadges" class="badges"></div></section>
<section class="card table-card"><div class="section">Table</div><div class="metrics" style="margin-top:12px"><div class="metric"><label>Players</label><strong id="playersMetric">0</strong></div><div class="metric"><label>Sigils</label><strong id="sigilsMetric">0</strong></div><div class="metric"><label>Host</label><strong id="hostMetric">None</strong></div><div class="metric"><label>Starter</label><strong id="starterMetric">None</strong></div></div><div style="margin-top:10px"><div class="status-row"><span>State</span><strong id="stateMetric">—</strong></div><div class="status-row"><span>Atlas firmware</span><strong id="firmwareMetric">—</strong></div><div class="status-row"><span>ESP-NOW</span><strong id="espMetric">—</strong></div></div></section>
<section class="card session-card"><div class="section">My seat</div><div id="sessionBox" class="session-box" style="margin-top:12px"><div class="session-head"><div><div id="sessionTitle" class="session-title">Not authenticated</div><div id="sessionMeta" class="small">Choose a seat below.</div></div><button id="logoutButton" class="bad" style="display:none" onclick="logoutSession()">Log out</button></div><div id="sessionControls" class="actions" style="display:none;margin-top:10px"></div><div id="profileBox" style="display:none"><div class="profile"><input id="profileName" maxlength="32" placeholder="Player name"><button onclick="saveName()">Save name</button></div><div class="profile"><input id="profilePin" type="password" inputmode="numeric" maxlength="8" placeholder="New 4-8 digit PIN"><button onclick="savePin()">Set PIN</button></div><button id="clearPinButton" class="bad" style="margin-top:8px;display:none" onclick="clearPin()">Remove PIN</button></div><div id="claimHelp" class="small" style="margin-top:10px">Each browser authenticates to a logical seat. A shared Sigil can have separate A and B browser sessions.</div><div id="toast"></div></div></section>
<section class="card devices-card"><div class="section">Device manager</div><div id="atlasDevice" class="notice" style="margin:12px 0">Loading Atlas identity...</div><div id="devices" class="devices"></div></section>
<section class="card players-card"><div class="section">Players</div><div id="players" class="players" style="margin-top:12px"></div></section>
<section class="card system-card"><div class="section">System</div><div style="margin-top:10px"><div class="status-row"><span>Active player</span><strong id="activeMetric">None</strong></div><div class="status-row"><span>Winner</span><strong id="winnerMetric">None</strong></div><div class="status-row"><span>Win response</span><strong id="confirmMetric">None</strong></div><div class="status-row"><span>Master button</span><strong id="masterMetric">Released</strong></div><div class="status-row"><span>Atlas OTA</span><strong id="otaMetric">Locked</strong></div></div></section>
</div></div>
<script>
let statusData=null,deviceData=null,seatData={seats:[]},sessionInfo=null,pendingClaim=null,pendingTimer=null;
let sessionToken=localStorage.getItem('turnhubSessionToken')||'';
function esc(v){return String(v??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));}
function badge(t,c=''){return `<span class="badge ${c}">${esc(t)}</span>`}
function authHeaders(){return sessionToken?{'X-TurnHub-Token':sessionToken}:{}}
function seatByPlayer(n){return seatData.seats.find(s=>Number(s.player)===Number(n))}
function playerLabel(n){if(!Number(n))return 'None';const s=seatByPlayer(n);return s&&s.name?s.name:`Player ${n}`}
function toast(msg,bad=false){const e=document.getElementById('toast');e.textContent=msg;e.style.color=bad?'var(--bad)':'var(--muted)'}
function renderStatus(s){statusData=s;dot.className='dot online';connection.textContent='Connected';playersMetric.textContent=s.players;sigilsMetric.textContent=s.sigils;hostMetric.textContent=Number(s.host)>=0?`Sigil ${Number(s.host)+1}`:'None';starterMetric.textContent=playerLabel(s.starter);stateMetric.textContent=s.state;firmwareMetric.textContent='v'+s.firmware;espMetric.textContent=s.espNow?'Ready':'Error';activeMetric.textContent=playerLabel(s.active);winnerMetric.textContent=playerLabel(s.winner);confirmMetric.textContent=playerLabel(s.winConfirm);masterMetric.textContent=s.masterButton?'Pressed':'Released';otaMetric.textContent=s.otaStateAllowed?'State ready':'Game locked';let title='READY',sub='Press Action on a Sigil to join',eye=s.state,bs='';if(s.state==='LOBBY'){title=s.players?`${s.players} PLAYER${Number(s.players)===1?'':'S'}`:'READY';sub=s.starter?`${playerLabel(s.starter)} goes first`:'Press Action on a Sigil to join';if(Number(s.host)>=0)bs+=badge(`Sigil ${Number(s.host)+1} host`,'good');if(s.starter)bs+=badge(`${playerLabel(s.starter)} starter`,'blue')}else if(s.state==='STARTING'){title='3 · 2 · 1';sub=`${playerLabel(s.starter)} starts`;bs+=badge('Countdown','blue')}else if(s.state==='RUNNING'){eye='ACTIVE TURN';title=s.active?playerLabel(s.active).toUpperCase():'RUNNING';sub='Table is live';bs+=badge('Running','good')}else if(s.state==='PAUSED'){title=s.winConfirm?'RESPOND':'PAUSED';sub=s.winConfirm?`${playerLabel(s.winConfirm)} must confirm or deny`:s.eliminationTarget?`${playerLabel(s.eliminationTarget)} selected`:'Game paused';bs+=badge('Paused','warn')}else if(s.state==='GAME_OVER'){title=s.winner?playerLabel(s.winner).toUpperCase():'COMPLETE';sub=s.winner?'Winner confirmed':'Game complete';bs+=badge('Game over','good')}eyebrow.textContent=eye;heroTitle.textContent=title;heroSub.textContent=sub;heroBadges.innerHTML=bs;renderPlayers();renderSession()}
function renderPlayers(){const cards=seatData.seats.map(s=>{let cls='player';if(s.active)cls+=' active';if(statusData&&Number(statusData.winner)===Number(s.player))cls+=' winner';if(s.eliminated)cls+=' eliminated';let state=s.eliminated?'Conceded / eliminated':s.active?'Active turn':statusData&&Number(statusData.winConfirm)===Number(s.player)?'Response needed':'At table';return `<div class="${cls}"><div class="player-name">${esc(s.name||`Player ${s.player}`)}</div><div class="small">Player ${s.player} · Sigil ${Number(s.module)+1} · Seat ${s.slotName}</div><div class="small" style="margin-top:8px">${esc(state)}${s.hasPin?' · PIN':''}${s.sessionClaimed?' · Browser linked':''}</div></div>`});players.innerHTML=cards.length?cards.join(''):'<div class="small">No players have joined yet.</div>'}
function seatsForModule(id){return seatData.seats.filter(s=>Number(s.module)===Number(id))}
function renderDevices(d){deviceData=d;atlasDevice.innerHTML=`<strong>Atlas</strong> · ${esc(d.atlas.hardwareId)} · firmware ${esc(d.atlas.firmware)}`;devices.innerHTML=d.devices.length?d.devices.map(x=>{const state=x.online?'<span class="device-state online-text">ONLINE</span>':'<span class="device-state offline-text">OFFLINE</span>';const fw=x.metadata?`Firmware ${esc(x.firmware)}`:'Firmware metadata unavailable';const seats=seatsForModule(x.id);const seatHtml=seats.length?seats.map(s=>{const current=sessionInfo&&Number(sessionInfo.module)===Number(s.module)&&Number(sessionInfo.slot)===Number(s.slot);let controls=current?'<button disabled>Current seat</button>':`<button ${x.online?'':'disabled'} onclick="claimSeat(${s.module},${s.slot})">Use ${s.slotName}</button>`;if(s.hasPin&&!current)controls+=`<button onclick="pinLogin(${s.module},${s.slot})">PIN login</button>`;return `<div class="seat ${s.active?'active':''}"><div class="seat-top"><div><span class="slot">${s.slotName}</span><strong>${esc(s.name||`Player ${s.player}`)}</strong></div><span class="small">P${s.player}</span></div><div class="actions" style="margin-top:8px">${controls}</div></div>`}).join(''):'<div class="small">Not joined to this game.</div>';return `<div class="device"><div class="device-top"><div><div class="device-name">${esc(x.label)}</div><div class="mono">${esc(x.hardwareId)}</div></div>${state}</div><div class="small" style="margin-top:8px">${fw}<br>Last seen ${Math.round(Number(x.ageMs)/100)/10}s ago · ${Number(x.sessionCount)} browser session${Number(x.sessionCount)===1?'':'s'}</div><div class="seats">${seatHtml}</div></div>`}).join(''):'<div class="small">No Sigils discovered yet.</div>'}
function renderSession(){const authed=!!(sessionInfo&&sessionInfo.authenticated);if(!authed){sessionTitle.textContent='Not authenticated';sessionMeta.textContent='Choose a seat in Device Manager.';sessionControls.style.display='none';profileBox.style.display='none';logoutButton.style.display='none';claimHelp.style.display='block';return}const name=sessionInfo.name||`Player ${sessionInfo.player}`;sessionTitle.textContent=name;sessionMeta.textContent=`Player ${sessionInfo.player} · Sigil ${Number(sessionInfo.module)+1} · Seat ${sessionInfo.slotName}`;logoutButton.style.display='inline-block';claimHelp.style.display='none';profileBox.style.display='block';if(document.activeElement!==profileName)profileName.value=sessionInfo.name||'';clearPinButton.style.display=sessionInfo.hasPin?'inline-block':'none';const buttons=[];const s=statusData||{};if(s.state==='LOBBY')buttons.push(`<button class="primary" onclick="sendControl('starter')">I go first</button>`);if(s.state==='RUNNING'){buttons.push(`<button class="primary" ${sessionInfo.active?'':'disabled'} onclick="sendControl('pass')">Pass</button>`);buttons.push(`<button class="warn" onclick="sendControl('pause')">Pause</button>`);buttons.push(`<button class="good" ${sessionInfo.active?'':'disabled'} onclick="sendControl('win')">Claim Win</button>`);buttons.push(`<button class="bad" onclick="concede()">Concede</button>`)}else if(s.state==='PAUSED'){if(Number(s.winConfirm)===Number(sessionInfo.player)){buttons.push(`<button class="good" onclick="sendControl('confirm')">Confirm Win</button>`);buttons.push(`<button class="bad" onclick="sendControl('deny')">Deny Win</button>`)}else if(!s.winConfirm&&!s.eliminationTarget){buttons.push(`<button class="warn" onclick="sendControl('pause')">Resume</button>`);if(sessionInfo.active)buttons.push(`<button class="good" onclick="sendControl('win')">Claim Win</button>`);buttons.push(`<button class="bad" onclick="concede()">Concede</button>`)}}sessionControls.innerHTML=buttons.join('');sessionControls.style.display=buttons.length?'flex':'none'}
async function refreshSession(){if(!sessionToken){sessionInfo=null;renderSession();return}try{const r=await fetch('/api/session/me',{headers:authHeaders(),cache:'no-store'});if(!r.ok)throw 0;sessionInfo=await r.json()}catch(_){sessionToken='';sessionInfo=null;localStorage.removeItem('turnhubSessionToken')}renderSession()}
async function claimSeat(module,slot){toast(`Waiting for Sigil ${module+1}${slot===1?'A':'B'} authorization...`);try{const r=await fetch(`/api/session/request?module=${module}&slot=${slot}`,{method:'POST'});const d=await r.json();if(!r.ok)throw new Error(d.error||'Claim failed');pendingClaim=d.requestId;if(pendingTimer)clearInterval(pendingTimer);pendingTimer=setInterval(pollClaim,450);toast(`Press Action on Sigil ${module+1} within 30 seconds to authorize seat ${slot===1?'A':'B'}.`)}catch(e){toast(e.message,true)}}
async function pollClaim(){if(!pendingClaim)return;try{const r=await fetch(`/api/session/poll?id=${encodeURIComponent(pendingClaim)}`,{cache:'no-store'});const d=await r.json();if(r.status===404){clearInterval(pendingTimer);pendingClaim=null;toast('Authorization expired.',true);return}if(d.status==='approved'){clearInterval(pendingTimer);pendingClaim=null;sessionToken=d.token;localStorage.setItem('turnhubSessionToken',sessionToken);toast('Seat authorized.');await refreshAll()}}catch(_){}}
async function pinLogin(module,slot){const pin=prompt(`PIN for Sigil ${module+1} seat ${slot===1?'A':'B'}:`);if(pin===null)return;try{const r=await fetch(`/api/session/login?module=${module}&slot=${slot}&pin=${encodeURIComponent(pin)}`,{method:'POST'});const d=await r.json();if(!r.ok)throw new Error(d.error||'Login failed');sessionToken=d.token;localStorage.setItem('turnhubSessionToken',sessionToken);toast('PIN accepted.');await refreshAll()}catch(e){toast(e.message,true)}}
async function logoutSession(){try{await fetch('/api/session/logout',{method:'POST',headers:authHeaders()})}catch(_){}sessionToken='';sessionInfo=null;localStorage.removeItem('turnhubSessionToken');toast('Logged out.');await refreshAll()}
async function sendControl(name){try{const r=await fetch('/api/control/'+name,{method:'POST',headers:authHeaders()});const d=await r.json();if(!r.ok)throw new Error(d.error||'Control rejected');toast(d.message||'Control accepted');setTimeout(refreshAll,100)}catch(e){toast(e.message,true)}}
function concede(){if(confirm('Concede this player from the current game?'))sendControl('concede')}
async function saveName(){const name=profileName.value.trim();try{const r=await fetch('/api/session/profile?name='+encodeURIComponent(name),{method:'POST',headers:authHeaders()});const d=await r.json();if(!r.ok)throw new Error(d.error||'Could not save name');toast('Player name saved.');await refreshAll()}catch(e){toast(e.message,true)}}
async function savePin(){const pin=profilePin.value.trim();if(!/^\d{4,8}$/.test(pin)){toast('PIN must be 4 to 8 digits.',true);return}try{const r=await fetch('/api/session/profile?pin='+encodeURIComponent(pin),{method:'POST',headers:authHeaders()});const d=await r.json();if(!r.ok)throw new Error(d.error||'Could not set PIN');profilePin.value='';toast('PIN saved.');await refreshAll()}catch(e){toast(e.message,true)}}
async function clearPin(){if(!confirm('Remove the saved PIN for this seat?'))return;try{const r=await fetch('/api/session/profile?clearPin=1',{method:'POST',headers:authHeaders()});const d=await r.json();if(!r.ok)throw new Error(d.error||'Could not remove PIN');toast('PIN removed.');await refreshAll()}catch(e){toast(e.message,true)}}
async function refreshAll(){try{const [sr,dr,tr]=await Promise.all([fetch('/api/status',{cache:'no-store'}),fetch('/api/devices',{cache:'no-store'}),fetch('/api/seats',{cache:'no-store'})]);if(!sr.ok||!dr.ok||!tr.ok)throw 0;const s=await sr.json();const d=await dr.json();seatData=await tr.json();await refreshSession();renderStatus(s);renderDevices(d)}catch(_){dot.className='dot';connection.textContent='Disconnected'}}
refreshAll();setInterval(refreshAll,700);
</script>
</body>
</html>
)HTML";

const char DEV_HTML[] PROGMEM = R"HTML(
<!doctype html><html lang="en"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><meta name="theme-color" content="#0b0d11"><title>TurnHub Dev</title><style>:root{color-scheme:dark}body{margin:0;padding:18px;background:#0b0d11;color:#f3f5f7;font-family:ui-monospace,Consolas,monospace}main{max-width:1000px;margin:auto}a{color:#9fc0ff}pre{white-space:pre-wrap;overflow-wrap:anywhere;background:#141820;border:1px solid #2b3240;border-radius:14px;padding:14px}</style></head><body><main><h1>TurnHub Dev</h1><p><a href="/portal">Back to portal</a></p><h2>Status</h2><pre id="status">Loading...</pre><h2>Devices</h2><pre id="devices">Loading...</pre><h2>Seats</h2><pre id="seats">Loading...</pre><script>async function r(){for(const id of ['status','devices','seats']){try{const x=await fetch('/api/'+id,{cache:'no-store'});document.getElementById(id).textContent=JSON.stringify(await x.json(),null,2)}catch(_){document.getElementById(id).textContent='Disconnected'}}}r();setInterval(r,1000)</script></main></body></html>
)HTML";

}  // namespace TurnHubWeb
